# A quick guide on bootstrapping this app:

### `Clone the repository`

In the project directory, you should run:

### `npm install`

This will install all neccesary node modules

### `npm start`

This will start the development server
